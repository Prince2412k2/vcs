[project]
name = "vcs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []


[project.scripts]
vcs = "vcs.cli:main"

[dependency-groups]
dev = [
    "vcs",
]

[tool.uv]
package = true
